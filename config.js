// Configuration file for animation settings
// These values can be easily modified without changing the main code

// Whether confetti pieces should rotate while falling
export const rotate = true;

// Maximum number of confetti pieces
export const maxConfetti = 150;

// Size of confetti pieces (1 is default)
export const size = 1;

// How long confetti animation should run (in milliseconds)
export const confettiDuration = 2000;  // 2 seconds